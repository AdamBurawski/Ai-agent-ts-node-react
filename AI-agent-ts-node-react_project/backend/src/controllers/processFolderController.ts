import { Request, Response } from 'express';

class ProcessFolderController {
  async execute(req: Request, res: Response) {
    // implementacja
  }
}

export const processFolderController = new ProcessFolderController(); 